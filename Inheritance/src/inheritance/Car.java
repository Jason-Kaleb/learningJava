package inheritance;

/*
    We use the keyword extends and
    than the class name of the class
    we want to inherit. like below
*/

public class Car extends Vehicle 
{
    // Car will now have access to the Vehicles class attributes and methods
    // Car can also have its own attributes and methods in addition to the inherited attributes and methods
    int doors = 4;
    int wheels = 4;
}
